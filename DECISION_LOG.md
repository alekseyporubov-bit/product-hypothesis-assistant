# Decision Log

> Формат — `.tdpd/templates/decision-log.md`. Статусы: proposed / accepted / superseded / revoked.

| ID | Decision date | Status | Decision | Why / selection criterion | Evidence/findings | Context | Rejected alternatives | Reversibility/cost | Decided by | Breaks if changed |
|---|---|---|---|---|---|---|---|---|---|---|
| DL-001 | 15.09.2026 | accepted | Домножить `weighted_score` на 10 в `ScoringEngine.calculate_score()` (итоговый score на шкале 0–100) | Оценки факторов считаются по шкале 0–10, а веса `FACTOR_WEIGHTS` суммируются в 1.0, поэтому `weighted_score` выходил 0–10. Пороги рекомендаций (>=70 proceed, >=50 investigate) и документация подразумевают шкалу 0–100, из-за чего «proceed»/«investigate» были математически недостижимы — любая гипотеза получала «reject». Умножение на 10 восстанавливает документированную шкалу без изменения весов и логики факторов. | F001 (см. ниже): `calculate_score()` → максимум ~9.36 при 60 подтверждающих доказательствах; S001: `WEB_UI_GUIDE.md` «Score (0-100)», пороги 70/50; `README_PRODUCT_HYPOTHESIS_ASSISTANT.md` пример `overall_score: 78.5 / recommendation: proceed` | C001: хард-стоп `TDPD_AGENT_INSTRUCTIONS.md:120` — не менять формулу/веса скоринга без фиксации `DL-###` | (a) оставить шкалу 0–10 и переписать пороги на 7.0/5.0 — противоречит «0–100» в доке и UI; (b) перевести оценки факторов на 0–100 — эквивалентный результат, но больший дифф | cheap (одна строка); калибровка весов всё равно отложена по ТЗ п.8 | a.porubov (уточнение от 15.09.2026) | бизнес-метрика корреляции score↔реальность (целевая точность > 75%); UI-отображение score; сохранённые в `hypotheses_data.json` старые score (~0–10) станут «низкими» до повторной валидации |
| DL-002 | 15.09.2026 | accepted | Заменить итоговую формулу `ScoringEngine.calculate_score()` на confidence-weighted `overall_score = 100 × agreement × strength`; факторные оценки оставить как информационную разбивку (не влияют на итог) | Старая факторная формула (0–10 × `FACTOR_WEIGHTS`) игнорирует `confidence` и требует доказательств по всем шести факторам, поэтому хорошо подтверждённая гипотеза (несколько надёжных источников + много положительных доказательств) получала ~55/100 («investigate») вместо ~90. Требование ПМ: score ближе к 100 при нескольких надёжных подтверждающих источниках и многих положительных доказательствах; низкий score, если большинство доказательств опровергает проблему | F002 (см. ниже): 5 подтверждающих @0.9 + 3 авто-исследования — старый ~55.8, новый ~89; `avg_confidence` вычислялся, но не использовался, поэтому confidence не влиял на score | C001: хард-стоп `TDPD_AGENT_INSTRUCTIONS.md:120` — формула меняется после фиксации DL-002 | (a) калибровка весов факторов — не устраняет игнор confidence и штраф за неполноту факторов; (b) удалить разбивку по факторам — теряем детализацию UI; (c) смешанная факторно-эвиденсная формула — сложнее и менее прозрачна | средняя — меняется итоговая формула; старые score в `hypotheses_data.json` устаревают до повторной валидации; пороги 70/50 и разбивка сохранены | a.porubov (уточнение от 15.09.2026) | пороги рекомендаций 70/50; бизнес-метрика корреляции score↔реальность (целевая точность > 75%); UI-отображение score |
| DL-003 | 15.09.2026 | accepted | Убрать локальное JSON-хранение гипотез (`hypotheses_data.json`) — единственное хранилище PostgreSQL; без `DATABASE_URL` данные живут только в памяти процесса | Пользователь подтвердил, что JSON-хранение не используется после подключения PostgreSQL; на RelaxDev `hypotheses_data.json` не подтягивается, а эфемерная ФС контейнера стирала бы файл при редеплое. JSON-фолбэк = мёртвый код и ложная видимость персистентности (данные «сохраняются», но теряются) | F003 (см. ниже): в `product_hypothesis_assistant.py` были `STORAGE_FILE`/`save_to_file`/`load_from_file` с JSON-ветками, `_atomic_write_json`/`_storage_lock`/`_backup_corrupt_file`; PostgreSQL-путь (`_save_to_postgres`/`_load_from_postgres`) уже реализован и протестирован (`test_postgres_persistence.py`) | C001: хард-стоп `TDPD_AGENT_INSTRUCTIONS.md:119` — не терять данные гипотез без бэкапа/миграции (локальный `hypotheses_data.json` не удаляется; `migrate_json_to_postgres.py` сохранён) | (a) оставить JSON-фолбэк — противоречит требованию «PostgreSQL — единственный путь»; (b) удалить и миграцию — риск потери локальных данных без переноса | средняя — удалены JSON-ветки кода; миграция и локальный JSON-файл сохранены | a.porubov (запрос от 15.09.2026) | поведение при отсутствии `DATABASE_URL` (только память, без сохранения); миграция старых JSON-данных |
| DL-004 | 21.09.2026 | accepted | Добавить Google multi-user аутентификацию: у `Hypothesis` появляется поле `user_id`; все методы `HypothesisManager` принимают опциональный `user_id` и фильтруют по владельцу; защищённые `/api/*`-маршруты возвращают 401 без сессии; неавторизованные видят только экран входа; легаси-гипотезы без владельца (`user_id=""`) автоматически забирает первый вошедший пользователь | Продукт используют несколько человек, данные каждого должны быть изолированы; без входа нельзя показывать чужие гипотезы. Выбран Google OAuth (Google Identity Services на фронте + проверка ID token на бэке через tokeninfo + Flask-сессия), т.к. не требует хранения паролей и даёт готовый защищённый идентификатор (`sub`) | S004 (запрос): изолировать гипотезы/данные по пользователю; неавторизованные видят запрос входа вместо данных. F004: до правки `Hypothesis` не имел `user_id`, методы менеджера не проверяли владельца, `/api/*` были публичными. E2E-004: `test_multi_user_auth.py` (30 сценариев Red→Green: scoping, orphan claim, JIT, 401) | C: хард-стопы — авторизация/миграции/секреты; JIT-создание учёток (`UserManager.ensure_user`); сессия подписывается `SESSION_SECRET`; таблица `users` создаётся автоматически; `user_id` хранится в JSONB без разрушающей миграции | (a) Google-кнопка только на фронте без проверки на бэке — небезопасно (токен можно подделать); (b) логин/пароль — нужен парольный менеджмент и сброс; (c) единый общий пользователь — противоречит изоляции | средняя — `user_id` в JSONB обратно совместим (старые строки читаются как `""`); методы без фильтра сохраняют старое поведение; откат = убрать декоратор и поле | a.porubov (запрос) | изоляция данных по `user_id`; 401-гейтинг `/api/*`; привязка сиротских гипотез к первому пользователю; наличие `user_id` в `to_dict` |



| DL-005 | 21.09.2026 | accepted | Добавить вход/регистрацию по логину и паролю (альтернатива Google OAuth): `UserManager.register_user`/`authenticate_user`, пароль хранится хешем pbkdf2:sha256, базовые требования к сложности (≥8 символов, минимум одна буква и одна цифра, не равен логину), уникальный регистронезависимый логин, маршруты `POST /api/auth/register` и `POST /api/auth/password-login`, без сброса пароля | Регистрация Google-приложения занимает 4–6 недель, поэтому нужен способ входа без Google; пароли не храним открытым текстом (werkzeug pbkdf2:sha256 уже в зависимостях через Flask), логин уникален, сброс пароля пока не требуется | S005 (запрос): вход/регистрация по логину и паролю, базовые требования к сложности пароля, выход из сессии, без сброса. F005: до правки был только Google-вход (`/api/auth/login`), `User` не имел полей `login`/`password_hash`. E2E-005: `test_password_auth.py` (31 сценарий Red→Green) | C: хард-стопы — авторизация/секреты; `password_hash` никогда не попадает в `to_dict()` (API), а только в `to_storage_dict()` (БД); `session.clear()` при каждом входе (защита от session fixation); «холостой» хеш для одинаковой по времени проверки при неизвестном логине | (a) только Google — блокирует пользователей на 4–6 недель до регистрации приложения; (b) пароль открытым текстом — неприемлемо; (c) email+пароль вместо логина — ужесточает UX без необходимости; (d) сброс пароля сразу — отложен по ТЗ | low — обратно совместимо: Google-вход не изменён; новые поля `login`/`password_hash` имеют значения по умолчанию; откат = удалить маршруты и поля | a.porubov (запрос) | уникальность логина; сложность пароля; невозможность утечки `password_hash` через API; работоспособность Google-входа параллельно |
| DL-006 | 22.09.2026 | accepted | Заменить устаревшую настройку `app.config['JSON_AS_ASCII'] = False` на `app.json.ensure_ascii = False` (Flask 3.1 игнорирует старый ключ) и переписать UI-тесты `test_ui_web.py` с проверкой авторизации и основных use cases | JSON-ответы API отдавали кириллицу как `\uXXXX`-escape (ключ `JSON_AS_ASCII` удалён из Flask 3.x); UI-тесты не учитывали экран входа (auth-gate) и не проверяли сценарии авторизованного пользователя | S006 (запрос): автотесты должны проверять авторизацию и создавать тестовых пользователей для UI-сценариев. F006: `test_ui_web.py` (до правки) предполагал публичный доступ без входа; API отдавал escaped-юникод. E2E-006: `test_ui_web.py` (9 UI-сценариев Selenium, headless Chrome) | C: не менять логику авторизации/скоринга; правка только JSON-сериализации и тестов; `app.json.ensure_ascii` не влияет на API-контракт (тот же JSON, но с UTF-8) | (a) оставить escaped-юникод — тесты не могут сопоставить читаемый текст ошибок; (b) ручное декодирование в тестах — хрупко и маскирует проблему | low — не меняет семантику JSON; откат = вернуть старый ключ и удалить auth-проверки в тестах | a.porubov | читаемость JSON-ответов API; зелёные UI-тесты авторизации (неавторизованный видит gate, 401 для `/api/*`, регистрация/вход/logout, создание гипотезы после входа) |

## Трассируемость (S → F → DL → PROB → RULE → SCN → E2E)

- **S001** — `WEB_UI_GUIDE.md` (Сценарий 3: «Score (0-100)», пороги 70/50) и `README_PRODUCT_HYPOTHESIS_ASSISTANT.md` (пример `overall_score: 78.5`, `recommendation: proceed`).
- **F001** — `product_hypothesis_assistant.py:357-386`: `weighted_score = Σ(фактор(0–10) × вес)` при `Σвесов = 1.0` ⇒ диапазон 0–10; подтверждено измерением (максимум ~9.36 при 60 доказательствах).
- **DL-001** — домножить `weighted_score` на 10 (шкала 0–100).
- **PROB-001** — рекомендации `proceed`/`investigate` недостижимы; любой ввод даёт `reject`.
- **RULE-001** — итоговый score в диапазоне 0–100; `proceed` при >=70, `investigate` при 50–69, `reject` при <50.
- **SCN-001** — ПМ валидирует хорошо подтверждённую гипотезу → видит score >= 70 и рекомендацию «идти в разработку».
- **E2E-001** — `test_score_scale.py` (Red → Green).
- **S002** — `Evidence.confidence` (0–1) и `Evidence.supports`; требование ПМ: «score ближе к 100 при нескольких надёжных подтверждающих источниках и многих положительных доказательствах; низкий score при большинстве опровергающих».
- **F002** — `product_hypothesis_assistant.py:245-253` (старая версия): `avg_confidence` вычислялся, но не использовался; факторные оценки считались по количеству, а не по confidence; 5 подтверждающих @0.9 + 3 авто-исследования давали ~55.8/100 (investigate).
- **DL-002** — confidence-weighted формула `overall_score = 100 × agreement × strength` (см. строку таблицы).
- **PROB-002** — score занижается для хорошо подтверждённых гипотез и не реагирует на confidence; «proceed» почти недостижим при неполном наборе факторов.
- **RULE-002** — `overall_score = 100 × agreement × strength`, где `agreement = total_support / (total_support + contradict_weight)`, `strength = min(1, total_support / 8)`, `total_support = Σ confidence(support) + min(research_count, 5) × average_relevance`.
- **SCN-002** — ПМ добавляет несколько подтверждающих доказательств (confidence 0.9) и получает авто-исследования → score ≥ 85 и «proceed»; при большинстве опровергающих → score < 50 и «reject».
- **E2E-002** — `test_score_confidence.py` (Red → Green).
- **S003** — сообщение пользователя: JSON-хранение гипотез не используется после подключения PostgreSQL; `hypotheses_data.json` на RelaxDev не подтягивается.
- **F003** — `product_hypothesis_assistant.py` (до правки): `STORAGE_FILE = "hypotheses_data.json"`, JSON-ветки в `save_to_file`/`load_from_file`, `_atomic_write_json`/`_storage_lock`/`_backup_corrupt_file`; PostgreSQL-путь уже был реализован (`_save_to_postgres`/`_load_from_postgres`).
- **DL-003** — удалить локальное JSON-хранение, единственный store — PostgreSQL (см. строку таблицы).
- **PROB-003** — JSON-фолбэк даёт ложную видимость персистентности (на RelaxDev файл эфемерный, данные теряются при редеплое).
- **RULE-003** — `HypothesisManager` пишет/читает только PostgreSQL при наличии `DATABASE_URL`; без него данные не сохраняются (только память).
- **SCN-003** — ПМ создаёт гипотезу при заданном `DATABASE_URL` → после редеплоя гипотеза остаётся в `/api/list-hypotheses`.
- **E2E-003** — `test_postgres_persistence.py` (критический сценарий «сохранение + редеплой»); `test_score_scale.py`/`test_score_confidence.py` (без БД, в памяти).
- **S004** — требование пользователя: добавить multi-user Google-аутентификацию и изолировать данные каждого пользователя; неавторизованные видят запрос входа, а не существующие данные.
- **F004** — `product_hypothesis_assistant.py`/`app.py` (до правки): `Hypothesis` без `user_id`, методы менеджера не проверяли владельца, все `/api/*`-маршруты были публичными; фронтенд всегда грузил `/api/list-hypotheses` без входа.
- **DL-004** — добавить Google OAuth + `user_id`-изоляцию + 401-гейтинг + orphan-claim (см. строку таблицы).
- **PROB-004** — данные всех пользователей смешаны в одном списке; неавторизованные видят чужие гипотезы; нет границы между учётными записями.
- **RULE-004** — каждый объект `Hypothesis` несёт `user_id`; `list/get/add_evidence/validate/record_outcome/export/scan/correlation` принимают `user_id` и возвращают данные только владельца; `POST /api/auth/login` создаёт/находит учётку (JIT) и пишет `user_id` в сессию; защищённые маршруты без сессии → 401.
- **SCN-004** — неавторизованный открывает `/` → видит экран входа; после входа через Google видит только свои гипотезы; второй пользователь не видит гипотезы первого; легаси-гипотезы `user_id=""` становятся доступны первому вошедшему.
- **E2E-004** — `test_multi_user_auth.py` (30 сценариев: scoping, orphan claim, JIT-создание, 401-гейтинг).
- **S005** — требование пользователя: регистрация/вход по логину и паролю (альтернатива Google, т.к. регистрация Google-приложения занимает 4–6 недель); базовые требования к сложности пароля; выход из сессии; сброс пароля не нужен.
- **F005** — `auth.py`/`app.py` (до правки): был только Google-вход (`/api/auth/login`); `User` не имел полей `login`/`password_hash`; маршруты `/api/auth/register` и `/api/auth/password-login` отсутствовали.
- **DL-005** — добавить логин/пароль как второй способ входа (см. строку таблицы).
- **PROB-005** — пользователи не могут войти, пока Google-приложение не зарегистрировано (простой 4–6 недель).
- **RULE-005** — регистрация по уникальному логину (нормализованному к нижнему регистру, 3–64 символа, допустимы буквы/цифры/`._-@`) + пароль (≥8 символов, минимум одна буква и одна цифра, не равен логину), хеш pbkdf2:sha256; вход `POST /api/auth/password-login`; `password_hash` только в БД (`to_storage_dict`), не в API (`to_dict`); `session.clear()` при каждом входе; сброс пароля не реализуется.
- **SCN-005** — пользователь регистрируется по логину/паролю (сложность проверяется), выходит из сессии и снова входит по логину/паролю; при слабом пароле или занятом логине видит понятную ошибку; параллельно работает Google-вход.
- **E2E-005** — `test_password_auth.py` (31 сценарий Red→Green: валидация пароля/логина, регистрация, вход, дубликат, сосуществование Google+пароль, HTTP-уровень).
- **S006** — требование: автотесты должны проверять авторизацию; тестовые пользователи создаются для UI-сценариев.
- **F006** — `test_ui_web.py` (до правки) не учитывал экран входа (падал после введения auth-gate); API отдавал кириллицу как `\uXXXX` (Flask 3.1 игнорирует `JSON_AS_ASCII`).
- **DL-006** — `app.json.ensure_ascii = False` + auth-aware UI-тесты (см. строку таблицы).
- **PROB-006** — UI-тесты не покрывали авторизацию и use cases авторизованного пользователя; JSON-ответы с escaped-юникодом нечитаемы.
- **RULE-006** — `test_ui_web.py` создаёт уникального тестового пользователя и проверяет: экран входа, 401 для `/api/*`, регистрацию (слабый пароль/дубликат/валидный), создание гипотезы, logout, вход (неверный/верный пароль).
- **SCN-006** — неавторизованный видит экран входа; после регистрации/входа видит приложение и создаёт гипотезу; после logout снова экран входа; повторный вход восстанавливает данные.
- **E2E-006** — `test_ui_web.py` (9 UI-сценариев Selenium, headless Chrome).

